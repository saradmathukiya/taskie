import React from 'react';

const Settings = () => {
  return (
    <div className="settings">
      <h2>Settings</h2>
      {/* Add settings options here */}
    </div>
  );
};

export default Settings;